/*#include <iostream>
#include <fstream>

using namespace std;

int main() {
	char *ptr;
	int counter = 0, i = 0, k = 0;
	ifstream fin;
	fin.open("help.txt", ios_base:: in);
	char *buffer = new char[k];
	char **dictionary = new char* [counter];
	for (i = 0; i < counter; i++) {
		dictionary[i] = new char[k];
	}
	dictionary = (char**)calloc(counter, sizeof(char*));
	while (!feof) {
		while(*ptr != )
	}
	

}*/